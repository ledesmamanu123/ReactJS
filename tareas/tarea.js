//Polyfill de FIND
const array1 = [1,5,101];

const encontrar = array1.find(elemento => elemento > 100)
console.log(encontrar)

array.prototype.customFind = function (callback){
    let resultado
    for (let i = 0; i < this.length; i++) {
        let elemento = this[i]
        
        
    }
}